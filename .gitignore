__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
*.db
aegis_cache.db
.DS_Store
.idea/
.vscode/

# Frontend build artifacts. The compiled stylesheet (templates/styles.css)
# IS checked in so deployments don't need a Node toolchain. node_modules
# is not — re-run `npm install && npm run build:css` after changing
# templates/index.html or tailwind.config.js.
node_modules/

# Locally-rendered report PDF. Not committed; regenerated from
# docs/REPORT.md via pandoc when needed for submission.
docs/The_Aegis_Final_Report_v3.pdf
